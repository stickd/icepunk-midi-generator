spring.jpa.hibernate.ddl-auto=update

# Flyway baseline mode: treats any pre-existing schema as V1 so that Flyway
# can be introduced without re-running migrations on databases already created
# by Hibernate's ddl-auto=update.
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1